{
  "name": "charity-risk-intelligence",
  "version": "1.0.0",
  "description": "CRA T3010 circular gifting visualization — web server + React frontend",
  "main": "visualizations/server.js",
  "scripts": {
    "start": "node visualizations/server.js",
    "build:frontend": "cd funding-loops-app && npm install --legacy-peer-deps && npm run build",
    "build": "npm run build:frontend",
    "dev:frontend": "cd funding-loops-app && npm run dev",
    "dev:server": "node visualizations/server.js"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
