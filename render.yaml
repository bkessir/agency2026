services:
  - type: web
    name: charity-risk-intelligence
    runtime: node
    rootDir: app
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/stats
    envVars:
      - fromGroup: charity-db   # DB_CONNECTION_STRING lives in this Environment Group
      - key: NODE_ENV
        value: production
